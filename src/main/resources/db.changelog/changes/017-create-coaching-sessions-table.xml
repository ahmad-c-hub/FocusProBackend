<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <changeSet id="017-create-coaching-sessions-table" author="abd">

        <createTable tableName="coaching_sessions">
            <column name="id" type="BIGINT">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="INT">
                <constraints nullable="false"
                             foreignKeyName="fk_coaching_sessions_user"
                             references="users(id)"/>
            </column>

            <column name="session_type" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>

            <column name="conversation_history" type="TEXT"/>

            <column name="goals_snapshot" type="TEXT"/>

            <column name="session_date" type="DATE">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP"/>

            <column name="closed_at" type="TIMESTAMP"/>
        </createTable>

        <addAutoIncrement tableName="coaching_sessions"
                          columnName="id"
                          columnDataType="BIGINT"
                          generationType="IDENTITY"/>

        <rollback>
            <dropTable tableName="coaching_sessions"/>
        </rollback>

    </changeSet>

</databaseChangeLog>
