<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <changeSet id="028-create-daily-scores-table" author="abd">

        <preConditions onFail="MARK_RAN">
            <not><tableExists tableName="daily_scores"/></not>
        </preConditions>

        <createTable tableName="daily_scores">
            <column name="id" type="BIGINT">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="INT">
                <constraints nullable="false"/>
            </column>

            <column name="score_date" type="DATE">
                <constraints nullable="false"/>
            </column>

            <column name="total_points" type="FLOAT8">
                <constraints nullable="false"/>
            </column>

            <column name="last_updated" type="TIMESTAMP"/>
        </createTable>

        <addAutoIncrement tableName="daily_scores"
                          columnName="id"
                          columnDataType="BIGINT"
                          generationType="IDENTITY"/>

        <addUniqueConstraint tableName="daily_scores"
                             columnNames="user_id, score_date"
                             constraintName="uq_daily_score_user_date"/>

        <rollback>
            <dropTable tableName="daily_scores"/>
        </rollback>

    </changeSet>

</databaseChangeLog>
