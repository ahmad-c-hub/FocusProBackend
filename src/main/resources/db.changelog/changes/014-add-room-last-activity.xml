<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <changeSet id="014-add-room-last-activity" author="abd">
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists tableName="focus_rooms" columnName="last_activity_at"/>
            </not>
        </preConditions>

        <!-- Tracks the last time someone joined; used for 2-day inactivity cleanup -->
        <addColumn tableName="focus_rooms">
            <column name="last_activity_at" type="TIMESTAMP"/>
        </addColumn>

        <rollback>
            <dropColumn tableName="focus_rooms" columnName="last_activity_at"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
